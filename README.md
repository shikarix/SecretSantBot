# Secret Santa Telegram Bot

Telegram-бот для организации игры в Тайного Санту на C# с использованием SQL Server и Dapper.

## Возможности

- 🏠 Создание комнат для игры
- 👥 Приглашение участников через коды
- 🎲 Автоматический розыгрыш (распределение участников)
- 📝 Просмотр назначений (кому дарить подарок)
- 💾 Хранение данных в SQL Server

## Технологии

- .NET 9.0
- Telegram.Bot
- Dapper (для работы с БД)
- SQL Server
- Docker & Docker Compose

## Установка и запуск

### Требования

- .NET 9.0 SDK
- Docker и Docker Compose
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

### Шаги

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd SecretSantaBot
   ```

2. **Настройте переменные окружения:**
   
   Создайте файл `.env` в корне проекта:
   ```env
   TELEGRAM_BOT_TOKEN=your_bot_token_here
   ```
   
   Или добавьте токен в `appsettings.json`:
   ```json
   {
     "TelegramBot": {
       "BotToken": "your_bot_token_here"
     }
   }
   ```

3. **Запустите приложение через Docker Compose:**
   ```bash
   docker-compose up -d
   ```
   
   Это запустит:
   - SQL Server
   - Бота (SecretSantaBot)

4. **Инициализация базы данных:**
   
   База данных будет создана автоматически при первом запуске SQL Server.
   Для создания таблиц выполните скрипт `SecretSantaBot/Scripts/init.sql` в SQL Server.
   
   Или подключитесь к SQL Server и выполните:
   ```sql
   USE master;
   CREATE DATABASE SecretSanta;
   ```
   Затем выполните скрипт из `SecretSantaBot/Scripts/init.sql`

### Запуск без Docker

1. **Настройте SQL Server:**
   - Установите SQL Server локально или используйте Docker:
     ```bash
     docker run -e "ACCEPT_EULA=Y" -e "MSSQL_SA_PASSWORD=YourStrong@Passw0rd" -p 1433:1433 mcr.microsoft.com/mssql/server:2022-latest
     ```

2. **Создайте базу данных:**
   - Подключитесь к SQL Server
   - Выполните скрипт из `SecretSantaBot/Scripts/init.sql`

3. **Настройте строку подключения:**
   
   В `appsettings.json` или `appsettings.Development.json`:
   ```json
   {
     "ConnectionStrings": {
       "DefaultConnection": "Server=localhost;Database=SecretSanta;User Id=sa;Password=YourStrong@Passw0rd;TrustServerCertificate=True;"
     }
   }
   ```

4. **Запустите приложение:**
   ```bash
   cd SecretSantaBot
   dotnet run
   ```

## Использование

### Команды бота

- `/start` - Начать работу с ботом
- `/help` - Показать справку по командам
- `/createroom <название>` - Создать новую комнату
- `/myrooms` - Показать список ваших комнат
- `/roominfo <room_id>` - Информация о комнате
- `/invite <room_id>` - Создать код приглашения
- `JOIN <код>` - Присоединиться к комнате по коду
- `/draw <room_id>` - Провести розыгрыш (только создатель)
- `/myassignment <room_id>` - Узнать, кому вы дарите подарок

### Пример использования

1. Создайте комнату:
   ```
   /createroom Новогодний обмен
   ```

2. Пригласите участников:
   ```
   /invite 1
   ```
   Поделитесь полученным кодом с участниками

3. Участники присоединяются:
   ```
   JOIN ABC123XYZ789
   ```

4. Когда все присоединились, проведите розыгрыш:
   ```
   /draw 1
   ```

5. Каждый участник узнает, кому дарить:
   ```
   /myassignment 1
   ```

## Структура проекта

```
SecretSantaBot/
├── Models/              # Модели данных
│   ├── Room.cs
│   ├── User.cs
│   ├── RoomParticipant.cs
│   ├── Assignment.cs
│   └── Invitation.cs
├── Repositories/        # Репозитории для работы с БД
│   ├── IRoomRepository.cs
│   ├── RoomRepository.cs
│   └── ...
├── Services/            # Бизнес-логика
│   ├── SecretSantaService.cs
│   └── TelegramBotService.cs
├── Scripts/             # SQL скрипты
│   └── init.sql
├── Program.cs           # Точка входа
└── appsettings.json     # Конфигурация
```

## Разработка

### Добавление новых команд

1. Добавьте обработчик в `TelegramBotService.cs`
2. Зарегистрируйте команду в `HandleCommandAsync`

### Расширение функционала

- Добавление списка желаний для участников
- Отправка напоминаний о дате обмена
- Групповые чаты (создание комнат в группе)
- Настройки ограничений стоимости подарков

## Лицензия

MIT

